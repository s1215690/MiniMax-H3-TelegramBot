# 雙卡調查報告（2026-09-15）

機器：2× RTX 3080 20GB（共 40GB，**無 NVLink / 無 CUDA P2P**）
結論：**雙卡生成目前無法完成。** 所有嘗試過的組合與失敗點都記錄如下。

---

## 一、結論摘要

| 組合 | 啟動 | 生成 | 失敗點 |
|---|---|---|---|
| 多卡 + DynamicVRAM | ✅ | ❌ | aimdo DLL 中止（無法捕捉） |
| 多卡 + `--disable-dynamic-vram` | ✅ | ❌ | VAE 解碼 OOM |
| 多卡 + DynVRAM + H3VM 双卡极速 | ✅ | ❌ | deepclone 成功、模型切分成功 → CLIP forward 中止 |
| 多卡 + 無DynVRAM + H3VM 双卡扩容 | ✅ | ❌ | deepclone 本身 access violation |
| 多卡 + aimdo 限單卡 + H3VM | ✅ | ⚠️ | **不崩，但 GPU1 閒置、任務無限卡住** |
| **單卡（現行）** | ✅ | ✅ | — |

---

## 二、關鍵事實

### 1. `--cuda-device all` 在 ComfyUI 0.31.0 是壞的

`ComfyUI-Turbo/main.py` L86-90：

```python
if args.cuda_device is not None:
    os.environ['CUDA_VISIBLE_DEVICES'] = str(args.cuda_device)   # 'all' 直接塞進去
```

說明文字聲稱支援 `all`，實作卻把字面字串賦值給 `CUDA_VISIBLE_DEVICES`。
CUDA 不認 `all` 這個 token → **程序看到 0 張卡**：

```
[INFO] Set cuda device to: all
comfy_kitchen backend cuda: {'available': False, 'unavailable_reason': 'CUDA not available'}
RuntimeError: No CUDA GPUs are available
```

**繞法**：用 `CUDA_VISIBLE_DEVICES=0,1` 即可。H3VM 原文也只要求「兩張卡可見」。

### 2. comfy-aimdo 多卡會中止整個程序

```
[ERROR] aimdo: src/hostbuf.c:283:ERROR:hostbuf_read_file_slice:
        device copy failed result=2 device_ptr=... device=0 stream=... size=67108864
Fatal Python error: Aborted
```

- **啟動時不會發生**（aimdo 對兩張卡都 `inited`、LUID 不同、無錯誤）
- **第一個 prompt 就中止** —— 而且失敗在 `device=0`，也就是單卡模式下完全正常的那張
- **0.4.15 與 0.5.3 都一樣**（升級救不了）
- abort 來自原生 DLL，**Python 無法 try/except**

`comfy_aimdo.host_buffer` 的參數註解提供了線索：

```python
ctypes.c_uint64,  # device dest ptr (0 = blocking host-only)
```

`device_ptr=0` 是純主機模式；崩潰時 `device_ptr` 非零 → 它在做裝置拷貝。

### 3. H3VM 本身運作正常，是被上下游夾殺

H3VM（`bt420000-collab/ComfyUI-H3-MultiGPU`，v0.21.1）**偵測與規劃都正確**：

```
[H3VM GPU PREFLIGHT] visible=2 | primary=cuda:0 | secondary=cuda:1 |
                     identical_models=yes | CUDA_VISIBLE_DEVICES='0,1'
[H3VM VRAM MASTER plan] Mode4 | symmetric=True | compute=50/50 blocks=25/25 heads=28/28
[H3VM Dev9.2 compute balance] effective split=25/25
[H3VM Dev9.2 registered islands] original=19.53GiB | primary=10.51GiB secondary=9.02GiB
[H3VM DEV9.4 RAM BACKED READY] cuda:1: blocks 0-24 | cuda:0: blocks 25-49 | weights 10.51+9.02GiB
```

**它真的把 19.53GB 的 UNET 切成 10.51 + 9.02 GiB 分到兩張卡。**

但它的 `core_adapter` 呼叫 `ModelPatcher.deepclone_multigpu()`：

```python
private = model.deepclone_multigpu(new_load_device=primary)   # ← 崩在這
```

- **DynVRAM 開** → deepclone 成功、模型切分成功 → 但 aimdo 在 CLIP forward 中止
- **DynVRAM 關** → deepclone 本身就 access violation

而 `deepclone_multigpu` 的實作會**再叫一次 loader 工廠**重載原始權重：

```python
temp_model_patcher = self.cached_patcher_init[0](*self.cached_patcher_init[1])
```

`cached_patcher_init` 是 DynamicVRAM 時代的機制 → 所以 H3VM 需要 aimdo，
但 aimdo 在多卡下會崩 → **邏輯死結**。

### 4. 手術式嘗試：繞開 aimdo 的檔案切片快路徑

`comfy/memory_management.py` 的 `read_tensor_file_slice_into()` 是 aimdo 的快路徑，
回傳 `False` 時呼叫端會退回普通 `copy_()`：

```python
if comfy.memory_management.read_tensor_file_slice_into(...):
    continue
dest_view.copy_(tensor, non_blocking=non_blocking)   # ← 安全的回退
```

在兩張卡可見時強制回傳 `False`：**`hostbuf_read_file_slice` 錯誤確實消失了**，
但中止只是**轉移到 CLIP 的量化 Linear forward**（`llama.py:645`）——
aimdo 的 staging 機制仍有其他多卡缺陷。

### 5. 最後一搏：讓 aimdo 只管一張卡

`main.py:278` 把**所有**可見裝置交給 aimdo。改成只給第一張
（重現已驗證可用的單卡形態），第二張完全交給 H3VM：

```
[WARNING] aimdo: 2 CUDA devices visible; initialising aimdo for device 0 only
aimdo: inited for GPU: NVIDIA GeForce RTX 3080
devices: 2                                    ← ComfyUI 仍看到兩張
```

**結果：不崩了，但也不工作。**

- GPU1 全程 **240 MiB / 0%** —— H3VM 切出來的島嶼沒有實際進駐
- GPU0 停在 9.8GB / 1-3% 使用率
- 日誌停止寫入，**卡死約 10 分鐘**，無產物
- `/queue`、`/history` 仍回 200（HTTP 層沒死，但工作停了）

推測：H3VM 的 relay 在等一個不會回應的 helper 裝置。

---

## 三、現行狀態（已復原）

| 項目 | 值 |
|---|---|
| ComfyUI | **0.35.0**（從 0.31.0 升級，upstream 官方 master） |
| comfy-aimdo | **0.5.3**（0.35.0 正好釘這個版本） |
| `app/logger.py` | 保留 `errors="replace"` 修正（cp950 emoji 防護） |
| 我加的兩處實驗性 patch | **已用 git checkout 還原** |
| `MINIMAX_COMFY_MULTIGPU` | **未設定**（單卡預設） |
| H3VM | 已安裝但**惰性啟動**，不執行它的節點就完全無作用 |

驗證：Bot 工作流在 0.35.0 上跑 **85.74 秒**成功出片；六個測試套件全綠。

---

## 四、若日後要再試

需要以下**任一**條件成立才值得重試：

1. **comfy-aimdo 修好多裝置 hostbuf**（追蹤 `hostbuf_read_file_slice`）——
   這是最根本的一條，修好後「多卡 + DynVRAM + H3VM 双卡极速」應該就能通
2. **H3VM 拿掉對 `deepclone_multigpu` 的依賴**，或提供不需
   `cached_patcher_init` 的路徑
3. **ComfyUI 修好 `--cuda-device all`**（雖然可用 `CUDA_VISIBLE_DEVICES` 繞過，
   但那是 plugin 的官方指示）
4. **H3VM 針對無 P2P 的 Windows 消費級雙卡補上 transport**
   （它自稱 `NO-D2D host_only` 有處理，但實測 relay 不回應）

**不建議**再花時間的方向：升級 aimdo 版本（已試 0.4.15 → 0.5.3 無效）、
升級 ComfyUI 版本（0.31 → 0.35 無效）。

---

## 五、附帶收穫（與雙卡無關但已生效）

1. **ComfyUI 從 0.31.0 升級到 0.35.0** —— 落後約 5 週的官方 master，
   本地只有 7 處修改（多為刪掉的 LTX blueprint），已安全合併
2. **comfy-aimdo 0.4.15 → 0.5.3** —— 與 0.35.0 的釘版一致
3. 修掉 Bot 的 `subprocess` 區域變數遮蔽 bug（原版潛藏）
4. 顯存保護（`llm_start_blocker`）—— 防止在 ComfyUI 佔用時硬啟 LLM
5. 導演台（`ComfyUI_MiniMaxH3_Director`）安裝與實測 —— 262s → 112s（加 Turbo LoRA 8 步）
